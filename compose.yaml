version: "3.9"

services:
  kua_bot:
    build: ./
    networks:
      - prismbot
    privileged: true
    restart: always
    volumes:
      - /etc/localtime:/etc/localtime:ro
  kua_db:
    image: "mongo:latest"
    networks:
      - prismbot
    privileged: true
    restart: always
    volumes:
      - ../db_data:/data/db
    ports:
      - "27018:27017"
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=qwer11!!

networks:
  prismbot:
